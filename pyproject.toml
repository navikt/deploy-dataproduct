[project]
name = "deploy-dataproduct"
version = "1.0.0"
description = "Catch all the deployments"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "google-cloud-bigquery>=3.41.0",
    "gunicorn>=25.3.0",
    "load-dotenv>=0.1.0",
]
